import {StyleSheet} from 'react-native';

const viewStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
    alignItems: 'center'
  },
  inputContainerStyle: {
    flexDirection: 'row', 
    alignItems: 'stretch'
  },
});

export default {
  styles : viewStyles
}
