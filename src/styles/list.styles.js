import {StyleSheet} from 'react-native';

const listStyles = StyleSheet.create({
  list_item: {
    backgroundColor: '#fff',
    alignItems: 'center',
  },
  list_text: {
    fontSize: 20
  }
});

export default {
  styles : listStyles
}
